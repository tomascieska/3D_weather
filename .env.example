# Server-side only (used by netlify/functions/weather.js).
# Do NOT prefix this with VITE_ — a VITE_ prefix would bundle it into
# the client-side JS and expose it publicly.
WEATHERAPI_KEY=your_weatherapi_key_here
